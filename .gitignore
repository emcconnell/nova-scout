# Godot 4 — generated cache and import data
.godot/
*.import
*.uid

# Export templates (large binaries — download via Godot editor)
export_templates/

# Build artifacts
builds/windows/
builds/macos/
builds/linux/
builds/web/

# macOS metadata
.DS_Store
**/.DS_Store

# Editor config (optional — remove if team shares editor settings)
.editorconfig

# GDScript LSP logs
*.log

# JetBrains / VSCode
.idea/
.vscode/
*.iml

# Godot local editor settings (machine-specific)
.local_editor_settings

# Temporary files
*.tmp
*.bak
